@model IEnumerable<CastService.Web.ViewModels.Equipments.ListEquipmentsViewModel>

<h2>Артикули</h2>
<p>
    @Html.ActionLink("Създай", "Create", "Equipments", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Model)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Model)
        </td>
        <td>
            @Html.ActionLink("Редактиране", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Детайли", "Details", new { id=item.Id }) @*|
            @Html.ActionLink("Изтриване", "Delete", new { id=item.Id })*@
        </td>
    </tr>
}

</table>
